import React from 'react';
import { Text, View } from 'react-native';

const EditPage = ({ navigation, restaurant }) => {
  return (
    <View>
      <Text>EditPage</Text>
    </View>
  );
};

export default EditPage;
