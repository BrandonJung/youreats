import React from 'react';
import { Text, View } from 'react-native';

const FoodPage = ({ foodList }) => {
  return (
    <View>
      <Text>Food List</Text>
    </View>
  );
};

export default FoodPage;
